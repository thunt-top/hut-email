# Tencent Cloud API credentials, used to sign SES requests.
# Copy this file to config/secret.toml and fill in real values;
# secret.toml is gitignored and never committed.
secret_id = "your-secret-id"
secret_key = "your-secret-key"
